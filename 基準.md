# claude部　作業用 — 基準.md

最終更新: 2026-09-13
状態: 運用フェーズ(本番稼働中)

---

## 概要

最初は「人に見せるための作品置き場」だったが、現在はGitHubリポジトリ`sky-portfolio`として、
実際に動く本番SaaS「入力保存一覧アプリ」(月額課金の会員制メモ保存アプリ)を開発・運用している。
あわせて、Claude Codeとの毎日のやり取りを通じてSaaSに必要な技術を体系的に学ぶ
「クロードコード部」の教材置き場にもなっている。

---

## 主な取り組み(2系統)

### ① クロードコード部 SaaS本体編(全23回・完走済み)

入力保存一覧アプリを、フォーム→保存→一覧→編集削除の基本機能から、認証(メール確認・パスワードリセット)、
Stripe課金(Pro化・Webhook・解約)、マルチテナント設計、セキュリティ堅牢化(テナント越境ゼロ・レート制限・監査ログ)、
稼働監視(SLO・アラート)、管理画面、法務ドキュメント(利用規約・特商法表記・プライバシーポリシー)まで、
23回かけて本番相当のSaaSに仕上げた。Railway(プロジェクト`proactive-wholeness`、production/staging環境)にデプロイ済み。

未対応の残課題:
- Stripeは本番Liveモードではなくテストモードのまま(事業者審査が必要なため保留)
- 特商法表記など法務書類は`【要入力】`のプレースホルダーのまま(実情報の差し替えが必要)
- 独自ドメイン未取得(`*.up.railway.app`のまま)
- staging環境(`入力保存一覧アプリ-staging`等)が2026年9月上旬から復旧できず不調(本番には影響なし、低優先で調査継続中)

### ② クロードコード部 仕組みがわかる編(全12回・完走済み、第31〜42回)

①で作った本物のインフラ(Railway/Postgres/本番URL)を教材に、DNS・HTTP・データベース設計・SQL・
認証と認可・HTTPS・セキュリティ点検(秘密の置き忘れ・SQLインジェクション・XSS)・サーバーとクラウド・
ログとデバッグ・バックアップと障害対応・技術カルテ(卒業回)までの「仕組み」を、架空の例ではなく
実際のコマンド・実データで確認しながら学ぶ日次シリーズ。第42回の卒業回で、入力保存一覧アプリの
言語・インフラ・データ・セキュリティ・公開方法を1枚の`技術カルテ.md`にまとめ、弱点(テスト・個人情報・
ライセンス・AI利用料)の洗い出しと3分説明の台本作成まで完走した。各回の記録はArtifactとして作成し、
PDF化してこのフォルダにも保存している(例: `住所と電話帳.pdf`、`入力保存アプリのテーブル設計.pdf`、
`鍵マークの中身.pdf`、`秘密の置き忘れ点検.pdf`、`技術カルテ.pdf`)。

未対応の残課題(技術カルテで洗い出し済み):
- `入力保存一覧アプリ`に自動E2Eテストが無く、実ブラウザ確認はPlaywrightでの手動検証に依存
- リポジトリにLICENSEファイルが存在しない
- バックアップは`memos`テーブルのみ対象(`users`等への拡張は着手済み、`.github/workflows/backup.yml`は下書きのまま未稼働)

### ③ クロードコード部 独り立ち編(2026-09-14開始、第43回〜)

②の卒業後に始まった続編。一人前のSEとして「設計し・自動化し・運用し・見積もれる」を目指す。
第43回(アルゴリズムと計算量)で、本番Postgresの実行計画(EXPLAIN)から`memos.owner`に索引が無く
Seq Scanになっている実際の弱点を発見し、`migrations/0008_add_memos_owner_index.sql`を作成(次回デプロイで自動適用)。
ローカルの使い捨てDBで10万件規模のベンチマークを行い、索引あり/なしで約154倍の速度差を実測した。

---

## フォルダ構成(主なもの)

- `入力保存一覧アプリ/` — 本番運用中のSaaS本体(app.py, auth.py, billing.py, db.py, webhook_server.py, migrations/, tests/ など)
- `献立提案アプリ/` — 別プロジェクト(詳細未整理)
- `仕様書/` — 各アプリの仕様書・ロードマップ
- 各種PDF — 仕組みがわかる編の各回記録、開発フローの図解(コミット台帳、リリース手順、マルチテナント設計 等)
- `omikuji.html` / `omikuji-maker.html` / `自己紹介.html` / `shindan.html` / `index.html` — 初期に作った練習用Webページ

---

## 重要ファイル

| ファイル | 役割 |
|---|---|
| `基準.md` | このプロジェクトの正本(今回更新) |
| `CLAUDE .md` | About me(自己紹介メモ) |
| `入力保存一覧アプリ/app.py` | SaaS本体のメインアプリ(Streamlit) |
| `仕様書/入力保存一覧アプリ_ロードマップ.md` | 開発ロードマップ(①フォーム→②保存→③一覧→④編集削除、完了済み) |

---

## Git / デプロイ

- GitHubリポジトリ: `1970windsocks/sky-portfolio`(このフォルダがそのまま管理対象)
- コミットのユーザー名/メール: `1970windsocks` / `k_windsocks@yahoo.co.jp`
- コミット数: 46(2026-07-30の最初のセーブポイントから)
- 最新コミット: `7ef66ac`(2026-08-30, staging→mainマージ)
- デプロイ先: Railwayプロジェクト`proactive-wholeness`(production環境で本番稼働中、staging環境は不調)
- コミット/PRの作法: コミットメッセージ末尾に`Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`、
  PR本文末尾に`🤖 Generated with [Claude Code]`を付ける

---

## 禁止事項

- 重要ファイルの削除は必ず確認する
- `.env`やAPIキーを共有しない(実際に点検済み・漏れ0件 — 詳細は`秘密の置き忘れ点検.pdf`)
- 本番環境への破壊的操作(DB変更・環境変数変更など)は事前に確認する

---

## 現フェーズ

運用フェーズ:SaaS本体編(全23回)・仕組みがわかる編(全12回)ともに完走。本番稼働中、次の学習テーマは未定。

---

## 次のアクション

- [x] 自己紹介ページのデザイン調整 — 2026-07-30
- [x] Gitリポジトリ初期化・最初のコミット — 2026-07-30
- [x] 入力保存一覧アプリ ①〜④(フォーム/保存/一覧/編集削除) — 完了
- [x] クロードコード部 SaaS本体編 全23回 — 完走(認証・課金・セキュリティ・監視・法務まで)
- [x] クロードコード部 仕組みがわかる編 全12回(第31〜42回) — 完走(技術カルテ作成まで)
- [ ] staging環境の復旧(低優先・裏で調査継続)
- [ ] Stripe本番Liveモードへの切替(事業者審査後)
- [ ] 法務ドキュメントの`【要入力】`箇所を実情報に差し替え
- [ ] 独自ドメインを取得する場合、Railway側の設定を追加

---

## 作業ログタグ

`~/.claude/作業ログ/` で `[claude部　作業用]` を検索
